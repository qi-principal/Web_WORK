<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.adplatform.module.ad.mapper.AdvertisementMapper">


    <select id="getAll" resultType="com.adplatform.module.ad.entity.Advertisement">
        select * from ad_platform.advertisement where id in
        <foreach collection="index" separator="," item="ids" open="(" close=")">
            #{ids}
        </foreach>
    </select>
</mapper>